import std.io;
import std.map;

i32 main(){
	HashMap<str, str> ids = HashMap<str, str>.new();

	ids["wallahi"] = "balls";
	ids["kurwa"] = "bigballs";

	printf("%s:%s\n", "wallahi", ids["wallahi"]);

	return 0;
}
