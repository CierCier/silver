// Test: all syscall wrappers compile and link correctly
import std.sys.syscall;
import std.sys.linux;

pub void main() {
    // Test 1: raw syscall1 (Phase 1)
    syscall1(60, 42);

    // Test 2: typed sys_exit (Phase 2)
    sys_exit(42);
}
