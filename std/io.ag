// std/io.ag — Silver I/O: re-exports the std.io submodules.
//
// File/BufWriter/STDOUT/println live in std.io.file, the stdin token
// scanner in std.io.scanner, and the buffered byte reader in
// std.io.byte_stream.
import std.io.file;
import std.io.scanner;
import std.io.byte_stream;
