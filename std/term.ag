// std/term.ag — terminal support: ANSI emitters, key-input parsing,
// window size, raw mode, and pseudoterminal spawning.
//
// Higher layers (screen renderer, line editor) build on these primitives.
import std.term.ansi;
import std.term.keys;
import std.term.size;
import std.term.raw;
import std.term.pty;
import std.term.loop;
