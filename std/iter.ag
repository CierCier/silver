// IntoIterator: types that can be iterated over via into_iter(self).
// Required method: Iterator into_iter(self);
trait IntoIterator {
    type Item;
    type IntoIter;
}

// Iterator: types that yield values.
// Required method: Optional<T> next(self) / next(&mut self);
trait Iterator {
    type Item;
}
