// std/hash.ag — Hash trait for types that want custom hashing
// HashMap<K, V> uses @hash(key) by default, not this trait.
// Implement Hash for your type to override the byte-hash behavior.

trait Hash<Self> {
    i64 hash(Self self);
}
