import std.io;

//This is a Single Line Comment

/*	This is a multi line 
	Comment.
*/


func factorial(i32 n) i32 {
	if (n == 0) {
		return 1;
	}
	
	return n * factorial(n - 1);
}


func main(i32 argc, *i8 argv) i32  {

	let i32 x = 129;
	// or use hexadecimal
	let i32 y = 0x81;
	
	// or use scientific notation
	let f32 z = 1.29e2;

	// minimal wrapper for printf
	printf("Hello World!\n");
	
	// for loop
	for (let i32 i = 0; i < 10; i++) {
		printf("i = %d\n", i);
	}

	// while loop
	let i32 i = 0;
	while (i < 10) {
		printf("i = %d\n", i);
		i++;
	}
	

	
	
	return 0;
}
