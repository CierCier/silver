import std.io;

//This is a Single Line Comment

/*	This is a multi line 
	Comment.
*/


func main(string arg) i32  {
	let i32 x = 129;
	// or use hexadecimal
	let i32 y = 0x81;
	// or use scientific notation
	let i32 z = 1.29e2;

	let string s = "Hello World!";

	printf("%s", s);

	if (x == y) {
		printf("%d is equal to %d", x, y);
	}
	

	return 0;
}
