import std.io;


struct Point {
    i32 x;
    i32 y;
}

enum Color {
    Red;
    Green;
    Blue;
}

i32 main() {
    Point p = { .x = 10, .y = 20 };

    printf("%d %d\n", p.x, p.y);

    return 0;
}
