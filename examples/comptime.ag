import std.io;

f64 factorial(f64 n) {
    if (n <= 1.0) {
        return 1.0;
    }
    return n * factorial(n - 1.0);
}

i32 main() {
    printf("factorial(10): %.0f\n", factorial(10.0));
    printf("factorial(20): %.0f\n", factorial(20.0));

    return 0;
}
