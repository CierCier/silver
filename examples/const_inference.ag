import std.io;

i32 main() {
    i32 x = 10;
    i32 y = 20;
    y = 30;
    printf("x = %d, y = %d\n", x, y);

    i32 z = 40;
    i32* p = &z;
    *p = 50;
    printf("z via pointer: %d\n", z);

    return 0;
}
