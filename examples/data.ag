

import std.io;


func counter(i32 n) i32{
    static i32 count = 0;
    count = count + n;
    return count;
} 


func main() i32 {
    
    printf("Counter: %d\n", counter(1));
    printf("Counter: %d\n", counter(1));
    printf("Counter: %d\n", counter(1));
    return 0;
}