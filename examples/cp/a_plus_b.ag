// A + B — classic competitive programming warm-up.
//
// Reads two integers from stdin and prints their sum.
import std.io.scanner;

i32 main() {
    Scanner s = Scanner.stdin();
    i64 a = s.next_i64();
    i64 b = s.next_i64();
    @println("{}", a + b);
    s.drop();
    return 0;
}
